[build]
  command = "npm install --prefix Learning-Management-System-main/frontend && npm run build --prefix Learning-Management-System-main/frontend"
  publish = "Learning-Management-System-main/frontend/build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
# Netlify config (root) — build from frontend directory
[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "build"

# SPA redirects - all routes should go to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false


